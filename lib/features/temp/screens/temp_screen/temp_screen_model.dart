import 'package:elementary/elementary.dart';

/// Default Elementary model for TempScreen module
class TempScreenModel extends ElementaryModel {
  ///
  TempScreenModel(
    ErrorHandler errorHandler,
  ) : super(errorHandler: errorHandler);
}
